#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:Cliente/node_modules/.bin/acorn.ps1
    $input | & "$basedir/node$exe"  "$basedir/../acorn/bin/acorn" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../acorn/bin/acorn" $args
========
    $input | & "$basedir/node$exe"  "$basedir/../nodemon/bin/nodemon.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../nodemon/bin/nodemon.js" $args
>>>>>>>> 6dcd462de3267c3bc639a50732e1ad085e9af9fa:Servidor/node_modules/.bin/nodemon.ps1
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:Cliente/node_modules/.bin/acorn.ps1
    $input | & "node$exe"  "$basedir/../acorn/bin/acorn" $args
  } else {
    & "node$exe"  "$basedir/../acorn/bin/acorn" $args
========
    $input | & "node$exe"  "$basedir/../nodemon/bin/nodemon.js" $args
  } else {
    & "node$exe"  "$basedir/../nodemon/bin/nodemon.js" $args
>>>>>>>> 6dcd462de3267c3bc639a50732e1ad085e9af9fa:Servidor/node_modules/.bin/nodemon.ps1
  }
  $ret=$LASTEXITCODE
}
exit $ret
